<% provide(:title, "Login") %>

<% flash.each do |message_type, message| %>
	<div><%= message %></div>
<% end %>

<h1>Login</h1>
<%= form_for :session, url: login_path do |f| %>
	<p>
		<%= f.label :email %>
		<%= f.email_field :email %> 
	</p>
	
	<p>
		<%= f.label :password %>
		<%= f.password_field :password %>
	</p>

	<%= f.submit "Login" %>
<% end %>